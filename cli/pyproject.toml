[project]
name = "x-media-archiver-cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "typer",
  "rich",
  "pydantic",
  "pydantic-settings",
  "orjson",
  "psycopg[binary]",
  "python-dotenv",
  "gallery-dl",
  "yt-dlp",
]

[tool.ruff]
line-length = 100
target-version = "py312"

